# План работ по развитию SPOD-консолидации

Легенда: **[сделано]** — реализовано в коде или закрыто проверками; **[частично]** — частично или только вручную; **[не сделано]** — в бэклоге.

---

## Фаза 1. Стабилизация текущей реализации

| № | Задача | Статус |
|---|--------|--------|
| 1.1 | Проверка корректности объединения на реальных изменённых данных между стендами | **[сделано]** (регрессия S2, S3, S4 в `Docs/TestReports`) |
| 1.2 | Проверка применения имён файлов из `config.json` по каждому стенду | **[сделано]** (входит в pre-check и ETL) |
| 1.3 | Валидация `source_stands` для всех 10 сущностей | **[частично]** (проверено на реальных прогонах; отдельного unit-пакета по 10 сущностям нет) |
| 1.4 | Дедупликация: повторный запуск без изменений; запуск с частично новыми файлами | **[сделано]** (логика SQLite + ручные/регрессионные прогоны) |
| 1.5 | Pre-check: отсутствие одного/нескольких файлов; отсутствие каталога стенда | **[сделано]** (сценарий S1) |
| 1.6 | Консольный отчёт: старт, файлы, статус БД, статистика разночтений | **[сделано]** |

---

## Фаза 2. Расширение надёжности

| № | Задача | Статус |
|---|--------|--------|
| 2.1 | Проверки схемы CSV по обязательным колонкам | **[не сделано]** |
| 2.2 | Контроль кодировок и обработка битых строк | **[частично]** (стандартный `csv`; явной политики «битых» байт в коде нет) |
| 2.3 | Расширить unit/integration тесты | **[частично]** (3 unit + ручные интеграционные сценарии; см. `test_plan_detailed.md`) |
| 2.4 | Тесты на правила подсветки строк и параметры форматирования из `config.json` | **[не сделано]** |

---

## Фаза 3. Расширение отчётности

| № | Задача | Статус |
|---|--------|--------|
| 3.1 | Лист Excel `DIFF_REPORT` со сводкой расхождений | **[сделано]** (`excel.diff_report_sheet` в `config.json`) |
| 3.2 | Экспорт технического отчёта в JSON/CSV | **[не сделано]** |
| 3.3 | Агрегаты по совпадениям/расхождениям по стендам (отдельный экспорт или лист) | **[частично]** (есть `SUMMARY` и консоль; отдельного агрегирующего листа «только по стендам» нет) |
| 3.4 | Детализация разночтений до уровня полей по одному ключу | **[частично]** (колонки `diff_*` на листах сущностей и в `DIFF_REPORT`; отдельного UI мастера нет) |

---

## Фаза 4. Эксплуатация

| № | Задача | Статус |
|---|--------|--------|
| 4.1 | Регламент запуска и проверки | **[частично]** (описано в README; формальный регламент организации — вне репозитория) |
| 4.2 | Версионирование схемы БД (миграции) | **[не сделано]** |
| 4.3 | План архивирования старых запусков/логов | **[не сделано]** (ручная уборка каталогов) |
| 4.4 | Versioned-шаблоны `config.json` для разных контуров | **[не сделано]** (единый `config.json` в корне) |

---

## Связанные документы

- Дорожная карта с этапами 0–9: `Docs/Roadmap_SPOD.md`
- Техническое задание: `Docs/ТЗ_SPOD_Консолидация.md`
- Предложения по улучшению: `Docs/TestReports/improvement_proposals.md`
